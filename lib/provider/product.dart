class Product {
  /*String? quantity, price, name, description, category, url;

  Product({
    required this.quantity,
    required this.price,
    required this.name,
    required this.description,
    required this.category,
    required this.url,
  });
  Map<String, dynamic> toMap() {
    return {
      'quantity': quantity,
      "price": price,
      "name": name,
      "description": description,
      "category": category,
      "url": url
    };
  }*/
  Map<String, dynamic> mapProduct = {};
  Product({required this.mapProduct});
}
